apiVersion: apps/v1
kind: Deployment
metadata:
  name: aura-frontend
  namespace: aura-ops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aura-frontend
  template:
    metadata:
      labels:
        app: aura-frontend
    spec:
      containers:
        - name: aura-frontend
          image: aura-frontend:local
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: aura-frontend
  namespace: aura-ops
spec:
  selector:
    app: aura-frontend
  ports:
    - port: 80
      targetPort: 80
