apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://bdev-prometheus:9090
    isDefault: true
    editable: true
    orgId: 1
    jsonData:
      timeInterval: "15s"
      queryTimeout: "60s"
      httpMethod: POST
